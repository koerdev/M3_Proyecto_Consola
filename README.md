# 🖥️ M3 - Proyecto Aplicación de Consola

Repositorio desarrollado para el **Módulo 3: Aplicación de consola**, donde se implementa una app ejecutable en navegador.

---

## Tecnologías utilizadas

- **JavaScript** (lógica principal)  
- **HTML** (estructura y UI básica)  
- Browser Console / DOM para interacción web

---

## Estructura del proyecto

El proyecto sigue la siguiente estructura:

- PM3/
    - index.html            # Interfaz principal de la consola
    - js/
        - leccion-1.js      # Lógica de la lección 1
        - leccion-2.js      # Lógica de la lección 2
        - leccion-3.js      # Lógica de la lección 3
        - leccion-4.js      # Lógica de la lección 4
        - leccion-5.js      # Lógica de la lección 5
    - README.md             # Documentación del proyecto

---

## Uso del proyecto

Para usar el proyecto:

1. Clona el repositorio
2. Abre el archivo index.html en tu navegador
3. Abre la consola de tu navegador para ver la interacción del código
4. En el archivo `index.html`, descomenta el script del cual deseas ver su funcionamiento en la consola  
5. Asegúrate de comentar los otros scripts para evitar conflictos

---

## Lecciones

- Lección 1: Introducción al lenguaje JavaScript
    - Escribir un script simple que muestre mensajes en la consola con `console.log()`.
    - Usar `prompt()` para recibir datos del usuario.
    - Mostrar mensajes personalizados con `alert()`.

- Lección 2: Variables, expresiones y sentencias condicionales
    - Definir variables utilizando `let` y `const`.
    - Pedir al usuario que ingrese dos números y almacenarlos en variables.
    - Implementar operaciones matemáticas (suma, resta, multiplicación, división).
    - Usar estructuras condicionales (if, else, switch) para evaluar diferentes situaciones.

- Lección 3: Arreglos y ciclos
    - Crear un arreglo con una lista de elementos.
    - Usar `for` y `while` para recorrer arreglos.
    - Implementar una función que filtre elementos según una condición.

- Lección 4: Funciones en JavaScript
    - Crear funciones para cada operación matemática.
    - Implementar una función que reciba parámetros y retorne un resultado.
    - Llamar funciones dentro de otras funciones para optimizar el código.

- Lección 5: Conceptos básicos de objetos en JavaScript
    - Crear un objeto con propiedades y valores.
    - Implementar métodos dentro de un objeto.
    - Usar un arreglo de objetos y recorrerlo con `map()` o `forEach`.